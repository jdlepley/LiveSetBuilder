<?xml version="1.0" encoding="utf-8" ?>
<Application
    x:Class="LiveSetBuilder.App.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Application.Resources>
        <ResourceDictionary>
            <!-- Palette -->
            <Color x:Key="Bg">#121212</Color>
            <Color x:Key="Panel">#1B1B1B</Color>
            <Color x:Key="PanelAlt">#232323</Color>
            <Color x:Key="Stroke">#333333</Color>
            <Color x:Key="Accent">#4E9BFF</Color>
            <Color x:Key="Text">#ECECEC</Color>
            <Color x:Key="TextSubtle">#B8B8B8</Color>

            <!-- Base styles -->
            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource Bg}" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Text}" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="TextColor" Value="{StaticResource Text}" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="10,8" />
            </Style>

            <!-- Card-like Frame -->
            <Style x:Key="CardFrame" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{StaticResource Panel}" />
                <Setter Property="CornerRadius" Value="14" />
                <Setter Property="HasShadow" Value="True" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="Margin" Value="0" />
            </Style>

            <!-- Small square icon buttons -->
            <Style x:Key="IconButton" TargetType="Button">
                <Setter Property="Padding" Value="0" />
                <Setter Property="WidthRequest" Value="36" />
                <Setter Property="HeightRequest" Value="36" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
